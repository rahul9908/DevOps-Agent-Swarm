# Observer Agent Dependencies
aiohttp==3.10.11
httpx==0.27.2
numpy==2.1.2
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=24.1.0
# The shared standard agent deps like NATS and SQLAlchemy are inherited from the monorepo root via pyproject.toml
